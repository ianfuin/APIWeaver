export interface RespModel {
  info?: string;
  success?: boolean;
}

/**
 * OK
 */
export type Response = RespModel;

const path = '/bichannel/status';
const method = 'get';
g;
  success?: boolean;
}

export type Body = HeartbeatModel;

/**
 * OK
 */
export type Response = RespModel;

const path = '/bichannel/heartbeat';
const method = 'post';
